apiVersion: v1
kind: Service
metadata:
  name: trust
  namespace: trust
  labels:
    app.kubernetes.io/part-of: arca-suite
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trust
  ports:
    - name: http
      port: 80
      targetPort: 8090
