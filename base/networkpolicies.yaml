# Default deny-all, then explicit allowances (zero trust).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trust-deny-all
  namespace: trust
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trust-allow-ingress
  namespace: trust
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: trust
  policyTypes: ["Ingress"]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              istio-injection: enabled
      ports:
        - port: 8090
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trust-allow-egress
  namespace: trust
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: trust
  policyTypes: ["Egress"]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: platform
      ports:
        - port: 9092
          protocol: TCP
        - port: 443
          protocol: TCP
